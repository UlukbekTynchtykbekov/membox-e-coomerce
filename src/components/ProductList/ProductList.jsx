import React from 'react';

const ProductList = () => {
    return (
        <>
            {/*{*/}
            {/*    data?.map(item => (*/}
            {/*        <ProductCard key={item.id} item={item}/>*/}
            {/*    ))*/}
            {/*}*/}

        </>
    );
};

export default ProductList;